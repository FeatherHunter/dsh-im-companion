/** truth-flags 唯一出口：仅注册逻辑，无样式无 DOM，slots 空数组。 */
import type { FeatureManifest } from '../protocol';

export const feature: FeatureManifest = {
  id: 'truth-flags',
  name: '真相旗标',
  order: 40,
  slots: [],
};
