# Handoff — dsh-im-companion · IM辅助面板黑屏修复与A1真数据化

> 交接给下一会话：本会话已完成从“设置面板不显示”到“IM机器人辅助 已亮起但主内容黑屏”的全链路对抗式修复，A1 已真数据化并自验通过。下一 Agent 凭本文档即可无缝继续，无需回翻历史。

**生成时间**: 2026-09-02 17:52:47 (Asia/Shanghai)  
**工作区**: `D:\dsh-plugin\dsh-im-companion` (`dsh-im-companion`)  
**Profile**: `desktop`（`C:\Users\辰辰洋洋\.dsh\profiles\desktop`）  
**上游**: `D:\dsh-plugin\dsh-im` (`FeatherHunter/dsh-im@0.15.4`, 零改动)  
**当前哈希**: `AEDDA51135C08A162DEB5C1203DFEF419AB9C55C`（`lib/client.js` 35.8k, `node --check` 0）

---

## 1. 背景与目标（Why）

* 原 handoff: `C:\Users\辰辰洋洋\AppData\Local\Temp\dsh-im-companion-handoff-2026-09-02.md` 定义最大全集 10 落点（A1/B1/B2/B3/C1a/C1b/D1/E2/E3/E4），`A1 三态聚合` 为首卡，落点 **设置 → IM机器人辅助**（原“试验田”，已更名）。
* 用户诉求：逐卡 **试 → 开发 → 真机验收 → 讨论 → 废弃/优化/换新**，当前聚焦 **desktop**，要求 **第一性原理对抗式审查，直至一定能显示成功，不让用户反复测**。
* 约束：不动 `dsh-im` 一行，`dsh-im-companion` 以 `slots.inject('settings.section', order:22)` 外挂。

## 2. 本会话已做（What & Why）

### 2.1 工程约定
* `setup-matt-pocock-skills`：`docs/agents/issue-tracker.md`（GitHub）、`triage-labels.md`（5+5）、`domain.md`（单上下文）、`AGENTS.md`（含 10 标签清单）、`CONTEXT.md`（Agent/Workspace/Bot/Channel 等术语）。

### 2.2 Wayfinder 图谱
* 地图：`.scratch/companion-wayfinder/map.md`（Destination 为 10 落点全部可验，`Notes` 覆盖 `Plan, don't do` 为决策+实现+预览验证）。
* 12 票：`.scratch/companion-wayfinder/issues/01-12-*.md`（`01-03 research`、`04,10 grilling`、`05-09,11 prototype`、`12 task`），阻塞已连边；`Context` 已建。
* 研究：`01 R1 workspaces/RPC` 已由子智能体产出 14k 研报（`~/.dsh/integrations/*/workspaces.json` + `connection.status`），`02 R2 Slots/DOM` 与 `03 R3 StateStore.sessionFor` 因 token 超限由本会话手工补齐（各 1.9k/2.3k），Frontier 现为 `01,02,03,12`。

### 2.3 更名与面板更名
* 包名：`@dsh-external/dsh-im-companion` → `dsh-im-companion`（`package.json:name`、`lib/*/PLUGIN_ID`、所有文档/ticket 已批量替换）。
* 面板名：`试验田` → `IM机器人辅助`（`lib/client.js` `label`/`h2`、`preview.html`、`AGENTS.md` 等）；用户最新要求 `Agent 助理·IM机器人辅助` → `IM机器人辅助` 已同步（`lib/client.js` 2 处、`preview.html` 2 处，哈希 `6775`→`9D43`→`AEDD`）。

### 2.4 DSH 启动异常三层修复（用户提供的总结已落地）
* **Profile 层**：`profiles/desktop|web/package.json` 双重 `dsh-im-companion`/`@dsh-external/dsh-im-companion` 已清，`bundles` 单一条 `dsh-im-companion`。
* **构建配置层**：`tsdown.config.ts:4` 已为 `dsh-im-companion`（原 `@dsh-external/dsh-agent-fleet`），`src/index.ts` 已重写为空壳 `dsh-im-companion`（与 `lib/index.js` 对齐）。
* **产物层**：`lib/client.js:558` 补 `}`（`  };
return` → `  };
}
return`），`{335/}334` → 平衡，`node --check` 0，新哈希 `FA7D6B455975...`（现 `AEDD` 经后续 A1 与日志追加）。
* **Bundle 校验**：`package.json` 补 `dsh.bundle: {patch:"./cordis.patch.yml"}`，`cordis.patch.yml` 先 `include: []` 后按 `overlay must be top-level array` 修正为 `[]`。

### 2.5 A1 三态聚合真数据化
* `package.json` `dsh.client.inject` 追加 `@deepseek-ai/dsh-client-connection`；`lib/client.js` `inject` 由 `['slots']` → `['slots','connection']`。
* `FleetPanel` 的 `// A1` 段（`MOCK_AGENTS` 静态）已替换为 **真数据版**：`ctx.connection.rpc.call('/'+ch,'connection.status')` 9 渠道并发、`AbortSignal.timeout`、按 `workspaceName`/`healthDot` 重分组，三胶囊带计数（`按Agent (n)` 等），`刷新` + 15s 语义，`loading/err/空态` 完整，`Mock` 回退。`a1RealBots` 等状态已用 `h` 版实现（非 `React.createElement` 版）。
* **关键修复**：`lib/client.js` 曾因 `tools.read` 截断（2034 字符）导致 `const CSS` 单行被截断为无效 token，已通过 `Copy-Item lib/client.js.fixed` + `[IO.File]::WriteAllText(..., UTF8NoBOM)` 恢复 33k `h` 版并重做 A1；后又因 `return {applyClient}` 与 `module.exports.apply` 双重嵌套导致 `settings.section` 未注册，已合并为单一 `module.exports.apply`（`applyClient` 2 处 → 1 处，`AEDD` 版已自验 `vm.runInNewContext` 捕获 `id:dsh-im-companion` 且 `apply` 注入 `settings.section` 成功）。

### 2.6 黑屏对抗式修复
* 用户反馈：重启后侧栏 **IM机器人辅助 已亮起**，但主内容 **漆黑一片**。
* 已加：`FleetPanel` 外层 `try/catch` 返回红框报错而非黑、`af-page` 强制 `background:#fff; color:#1f2329; min-height:400px` 深色主题可见、`Checklist` 的 `cb.addEventListener` 已验证 `document.createElement` 需 `addEventListener`（真实 DSH 有，mock 已补）、`e3Body.appendChild` 需 `append`+`appendChild` 已补。
* 已加日志探针：`apply` 首行 `console.log('[dsh-im-companion] apply start')` + 可视黄条 `[dsh-im-companion] apply ok`（5秒后消失）、`FleetPanel`/`Checklist` 各 `console.log`，哈希 `9D43`→`AEDD`。

## 3. 当前状态（Where）

* **文件**：`lib/client.js` 35.8k（`AEDD`）、`lib/index.js` 空壳 `dsh-im-companion`、`package.json` 含 `bundle`+`client`（`platform:web`，`desktop` 复用）、`cordis.patch.yml` 为 `[]`、`preview.html` 已更名。
* **Profile**：`desktop` `package.json` 单一条 `dsh-im-companion` 依赖+bundles，`node_modules/dsh-im-companion` 为 Junction 指向 `D:\dsh-plugin\dsh-im-companion`，三端哈希一致 `AEDDA511...`，`dev_install_package --profile desktop` 均 `client ✓`。
* **验证**：`node --check lib/client.js` 0；`vm.runInNewContext` 自验 `window.__ModuleLoader__.load id:dsh-im-companion` → `factory result [inject,apply]` → `inject settings.section` → `register IM机器人辅助` → `SUCCESS`（`FleetPanel` 在完整 mock 下亦 `SUCCESS`）。
* **UI 现状**：侧栏 **IM机器人辅助 已亮**（用户截图 1177x991 已确认），主面板点击后仍黑（用户最新反馈），已加黄条探针待 `Ctrl+F5` 后通过主面板全图或黄条判断 `apply` 是否调通。

## 4. 关键文件与命令（How）

* 插件：`D:\dsh-plugin\dsh-im-companion\lib/client.js`（主战场，`h` 版，`FleetPanel` + A1 真数据）、`lib/index.js`（空壳）、`package.json`、`cordis.patch.yml`、`preview.html`、`tsdown.config.ts`、`src/index.ts`（空壳）
* Wayfinder：`.scratch/companion-wayfinder/map.md`、`issues/06-p-tri-capsules.md`（A1）、`research/01-*.md` 等
* 校验：
  ```powershell
  Get-FileHash 'D:\dsh-plugin\dsh-im-companion\lib\client.js' -Algorithm SHA1
  node --check 'D:\dsh-plugin\dsh-im-companion\lib\client.js'
  # vm 自验见上
  ```
* 热装配：`dev_install_package --profile desktop`（`bundles` 需重启，`Ctrl+F5` 使 `rev` 生效）

## 5. 下一步契约（Who/When）

* 用户已在 **重启后 desktop 形态**，要求 **不让用户反复测**，本会话已承诺第一性原理自验。
* 下一 Agent 首务：**使 IM机器人辅助 主面板不再黑**，无需用户开 DevTools（`Ctrl+Shift+I` 不存在），通过 **黄条探针 + 主面板全图** 自验，或通过 **文件日志自验**（`vm` 已通，需补真实 DOM 探针）。
* 节奏：仍 **一次一卡**，当前卡 **A1 三态聚合**（`06`），待主面板显白卡后按 `按Agent/渠道/工作区` 验收计数，再收敛 `06` 并进 `C1b 矩阵`（`07`）。

## 6. 已知坑与规避

* **三者一致**：`package.json:name`、`tsdown PLUGIN_ID`、`bundles` 条目统一 `dsh-im-companion`，禁 scoped/unscoped 双重挂载。
* **手工改 `lib/client.js` 必跑 `node --check`**；`tools.read` 对超长行截断（2034 字符）会截断 `const CSS`，须用 `Get-Content -Raw -Encoding utf8` + `[IO.File]::WriteAllText(..., UTF8NoBOM)`。
* **双 `apply` 嵌套**：`h` 版 scaffold 的 `return {applyClient}` 与 `module.exports.apply` 不可共存，已合并。
* **深色主题**：`af-page` 需显式 `background:#fff`，否则侧栏黑底透出致“漆黑”。
* **Mock 完整性**：`document.createElement` 需 `addEventListener`/`append`+`appendChild`，`localStorage` 需 `try/catch`，`AbortSignal.timeout` 需降级。

## 7. Suggested Skills

* `dsh-plugin-ui-debug` — 用真实 Chrome (`ui_drive`/`ui_shot`) 对 **IM机器人辅助** 主面板做闭环调试（当前黑屏需此）
* `cordis-plugin-development` — Host/Client 双构建与 `connection.rpc` 真数据联调（A1 已用 `client-connection`）
* `wayfinder` — 认领并关闭 `06-p-tri-capsules`（A1），推进 `07-p-matrix-overview`（C1b）
* `domain-modeling` — 若需调整 `CONTEXT.md` 中 Agent/Workspace/Binding 术语

---
*Generated: 2026-09-02 17:52:47 | Workspace: D:\dsh-plugin\dsh-im-companion | Handoff for fresh session — 已对抗式审查并自验*
