# Handoff：#9 C1a 详情抽屉——待接上游真系统

## 1. 一句话状态（先读这段）

- 仓库：D 盘 dsh-plugin 下 dsh-im-companion，分支 master。Issue #9 进度 95%，实现已合入，未 close，等用户真机验收。

- 并行 session 同仓作业，HEAD 会变，动手前先看 git log。用户刚给新方向：预设/上下文改接 dsh-im 上游真系统（方案 A），等用户回开工二字，没回不动手。
- 接手第一件事：读 Issue #9 全文，再读 §5 待办，然后找用户确认 A 是否开工。

## 2. 已交付（B 变体）

- src/features/c1a/：manifest / data / view / drawer / styles，最大 222 行，红线 300。
- 新原语 ui/sheet.ts（右侧 sheet）。host 加 presets/ctxEnhance 持久化（A 方案要删）。
- 行入口详情按钮（§10 例外），panel.ts 零改动。verify 7 用例，npm run check 全绿是铁律。
- 与 B mock 的 8 项偏离：无保存/无+接入/换绑=路径输入/无清空解绑/路由空席/测试同 B3/自研 sheet/刷新保活。

## 3. 上游真相（P0-1 取证结论，直接用）

- 预设写：每渠道 bot.preset.set，payload 为 {botId, agentPreset}，agentPreset 只能是 null（跟随默认）或 DSH 预设 id。候选列表动态，从 DSH agentPresets 服务读，新会话生效，已有会话不受影响。
- 上下文写：每渠道 bot.context-enhancement.set，payload 为 {botId, config}，真 shape 是 {groupEnabled, directEnabled, fields, guidance}（群私聊各一开关）。
- 来源：dsh-im main 分支 plugin-src/host/channels/shared/ 下 agent-preset-rpc.mjs 与 context-enhancement-rpc.mjs，src/channels/shared/ 下同名核心文件。

## 4. A 方案（用户点头后按序做）

1. 读路径取证：抽屉要显示当前值，先找到读法。读不到就不写上下文（盲写覆盖用户引导语是正确性红线）。
2. 预设节：下拉改读动态 catalog＋跟随默认项，按 botId 写真实端点，删自持存档。
3. 上下文节：按真 shape 重做双开关，fields 与 guidance 只读不写。
4. 补 verify，check 全绿，显式 pathspec 提交，issue 记账。

## 5. 待办清单（按序）

1. 确认用户是否回了开工（没回就问，别自启实现）。
2. 若开工：按 §4 做完，约真机验收（完全退出重开＋Ctrl+F5，7 场景 v2 在本会话末尾消息里）。
3. 验收通过用户点头后才 close #9；close 顺手解锁 E3（#14 写着待 #9 关，路由 seat 接口就是抽屉里 fetchRoutesFor）。
4. 并行事项：Spec 复审子代理挂过两次（转自我覆盖）；dsh-im research 即 §4.1，已做完一半。

## 6. 坑与军规（逐条遵守）

- 并行 session 同仓：node_modules 被清过两次（坏了就 npm ci）；add 与 commit 只带显式 pathspec，禁用 -A；提交前看全量 status；绝不跨分支 checkout 或 stash；分支级操作走 plumbing，不动工作区。
- Windows：Python text 模式 stdin 会改换行，plumbing 一律 bytes；PowerShell 管道不传二进制（落盘或 worktree）；junction 写绝对路径。
- run_code 大程序体会截断致解析失败，单文件单调用；模板字面量一开一合别提前闭合。
- 截图通道返回缓存图，DOM 求值才是依据；真机以用户截图和 toast 原文为准。
- 共享层只加不改；样式 c1a 前缀；新原型放 prototypes 下；issue 用 body-file 写回（真实换行、无 BOM）；95% 写明待确认，未验收不 close。
- lib 未被跟踪靠 junction 生效：改完必须 check（含 build）再让用户重启。

## 7. 常用命令

- 全检 npm run check；单测 node --test tools/verify/features/c1a-drawer.ts；看票 gh issue view 9。
- 认领用引号：gh issue edit 9 --add-assignee 一对引号包住 @me（防 PowerShell 吃掉）。
- 用户真机：完全退出重开＋设置页 Ctrl+F5，只刷页面不够。

## 8. Suggested skills

- implement：开工后 TDD＋全绿＋code-review＋显式提交。
- research：读路径取证变复杂时转票沉淀。
- code-review：合入前双轴复审。
- grilling：再遇猜测先烤问。
- prototype：新设计问题开抛弃式原型（prototypes/c1a/，不进 main）。

## 9. 敏感信息

- 无。本仓无密钥，gh 走本机认证，无个人身份信息。
