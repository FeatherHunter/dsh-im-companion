Part of #1
## Question
功能即模块：规定每个功能的自包含包形态、共享包边界、挂载协议与并发开发规则，使 10 个功能可独立 session 并行开发。
## Context
R5 票研究结论为输入（各功能挂载点与共享文件触碰面清单）。现状：A1 的 panel.ts 342 行（红线 ≤300），src 已分 layers（ui 原语/data/主题/host 桥）。
## 待决
1. 目录形态：src/features/<feature>/{view,data,styles}.ts？挂载靠 manifest（feature.register）还是显式注入？
2. 共享包边界：哪些文件可动（theme.ts/dom/icons/config/meta/model？）哪些只能经导出接口？
3. 单份连接流 vs 每功能自取（多消费者订阅模式）——决定并发行与性能。
4. host 桥端点命名空间隔离（按功能前缀）+ 契约变更流程。
5. 300 行红线的精确口径（源码含注释？测试/工具豁免？构件产物豁免确认）。
## Done when
规则成文且形成「并发布局图」（哪些功能可同时开工、哪些共享点需先合并）；实现落到 CONTEXT.md + docs/features-contract.md。

## 开工必读

先读 docs/features-contract.md（§0 最高原则 + §1 解耦九律）；认领即 assign @me；只写自己的目录、共享只加不改；完工前 npm run check 全绿 + 可移除性试验。提审等用户点头后才可关闭。

## 进度：100%

下一步：已决议：契约落 docs/features-contract.md；无待办