Part of #1
## Question
左栏工作区顶部筛选条：全部/已绑定/未绑定三态切换 + 计数；与 DSH 左栏原生筛选的共存与冲突处理。
## Context
B1 徽标为前置（同一左栏区域）；绑定=Bot 已声明归属某 Agent 的 workspace（CONTEXT Binding）。
## Done when
真机筛选生效、计数准确、无布局回归；决策记录（是否委托原生筛选）。

## 开工必读

先读 docs/features-contract.md（§0 最高原则 + §1 解耦九律）；认领即 assign @me；只写自己的目录、共享只加不改；完工前 npm run check 全绿 + 可移除性试验。提审等用户点头后才可关闭。

## 进度：0%

下一步：待 B1（#6）关闭后认领