Part of #1

## Question

左侧工作区以 IM 徽标装饰：在线/离线/待确认三态外观（复用连接域数据），与会话区/设置页视觉一致；徽标可点击行为？

## Context

调研已含 MutationObserver 叠加方案 + 绿色呼吸点；A1 已形成连接轮询与状态映射。

## 原型（逻辑分支，grilling 已澄清）

分支判定：逻辑/状态模型是否顺畅 → 单文件可分享 HTML：prototype-b1-left-badge.html（仓库根目录，双击即开，零依赖，内存状态，刷新重置）。

已确认口径（Q1–Q4 全按推荐）：待确认 = 连接域 warn（degraded/checking/unknown），未绑定另算灰色；多渠道聚合 online > warn > offline（沿用 mergeStatus）；徽标点击只读跳转到 IM机器人辅助设置页对应 Agent 行（tooltip 显示 healthDetail，不做 mutation）。

覆盖场景：① 快乐路径（在线→轮询→点击跳转）；② 聚合边缘（online 优先、warn 次之、全离线才离线）；③ 只读与搬移（点击不改变连接·绑定指纹；同 Bot 改绑会搬移，旧工作区变未绑定）；④ 未知与时间（轮询失败→待确认+时间冻结，绝不谎报离线）。

验证：纯逻辑 21 断言全过 + 真机浏览器驱动四场景（状态外显、变化点、verdict 文本均正常）；截图 b1-s1-initial / b1-s2-after-steps / b1-s4-unknown。

## Verdict（已确认）

绿灯只承诺可达性（任一通道在线即在线），不承诺全通道健康；轮询失败 = 未知（待确认 + 时间冻结，绝不显示为离线）；tooltip 必带最后检测时间；点击只承诺打开设置页 section（#agent= 锚点待定）。

## Done when

真机左栏可见徽标、状态随 15s 轮询变化、深色适配、无布局回归；提供预览截图。

## 开工必读

先读 docs/features-contract.md（§0 最高原则 + §1 解耦七律）；认领即 assign @me；只写自己的目录、共享只加不改；完工前 npm run check 全绿 + 可移除性试验。提审等用户点头后才可关闭。

## 进度：95%

下一步：verdict 已确认，待实现落定（reducer 折叠进 bindings + overlay 引擎、补 #agent= 锚点或降级为只开 section）与真机验收（左栏可见、15s 跟随、深色、无回归、截图）；在实现合入前本 issue 不 close，原型归档抛弃分支并留指针。