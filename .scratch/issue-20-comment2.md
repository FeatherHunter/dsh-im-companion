转向记录（2026-09-04，用户结论）：新建机器人恒有默认归属，实践中不存在“未绑定池”。因此 E2 从“给未绑定分身份”转向“已分配之间重新调拨”：

- 入口：左栏头按钮“分身份”（仿左栏筛选第 4 按钮口径，不碰他人文件）。
- 形态：分组面板（未分配组置顶、无则隐藏；每组=归属，组头即大放置目标）；组间互拖换绑为主，每行“移交给”下拉兜底（触屏/拖不动场景）。
- 左栏 overlay（池条/行着色）已删除；看板列式否决（超 3 组即难用）。
- 不变：迁移表 7 行、换绑确认、5s 撤销（仅换绑）、拒绝语义、单接缝自验证。
- 行文本映射（resolveRowWorkspace）随 overlay 删除；面板按分组直连归属。

实现：097b98f（面板）+ f7a5596（panel.ts 拆分，300 行红线）。自验证 16 项全绿（隔离跑；committed 版待树绿重跑）。