# 审查 R5 · T7（#90）宿主侧：解耦合规与触碰面

审查人：对抗式审查员 R5（只审不改）。基线 HEAD `875c8ac`（工作树含 T4/T6 等并发未提交改动）。
方法：逐文件 `git diff` + 逐行读源码 + `npm run guard` / `tsc --noEmit` / `node --test tools/verify/features/update-host.ts` + 在**副本**里做可移除性验证（live 树零写入）。
被判对象：新增 `src/host/update.ts`、`update-reader.ts`、`update-schedule.ts`、`update-paths.ts`、`dsh-plugin-update.d.ts`、`tools/verify/features/update-host.ts`；修改 `src/index.ts`、`src/host/rpc.ts`、`src/host/meta-store.ts`、`tools/verify/host-entry.ts`、`package.json`（连带 `package-lock.json`）。

## 触碰面逐条裁定（只追加/违规）

| # | 位置 | 变更性质 | 裁定 |
|---|---|---|---|
| 1 | `src/index.ts:6` 增 import `createUpdateHostBridge` | 只加一行 | 只追加 |
| 2 | `src/index.ts:28` `createAgentFleetHandler(store)` → `(store, { update: () => update })` | **改写既有行**（兼容扩参：旧调用等价于默认 `{}`） | 追加式改写，非违规（既有端点分发路径逐字未动） |
| 3 | `src/index.ts:66-69` 让位分支之后建桥 + `ctx.effect` 释放 + `start()` | 只加 5 行；让位分支 `return` 早退时不可达（late-bound 提供函数） | 只追加 |
| 4 | `src/host/rpc.ts:6` 增 `import { isUpdateIntervalHours } from './meta-store.js'` | 只加一行导入 | 只追加 |
| 5 | `src/host/rpc.ts:16-30` 新接口 `UpdateHostSurface` / `AgentFleetHandlerOptions` | 只加导出（端口由本文件声明，不 import 任何 update 模块 → 依赖反向） | 只追加 |
| 6 | `src/host/rpc.ts:56` 既有导出 `createAgentFleetHandler` 的 `opts` 类型由内联 `{ dshHome?: string }` 换为 `AgentFleetHandlerOptions` | **唯一一处「改既有导出签名」**：超集 + 默认值不变 ⇒ 源码兼容、运行行为不变（全仓唯一调用点即 index.ts:28） | 字面非只追加 → **需在触碰面自述单列**；不判违规 |
| 7 | `src/host/rpc.ts:136-145` 追加 `case 'meta.update.set'`；`:176-186` 追加 `case 'imc.updateStatus/updateCheck/updateInstall'` | 纯 append 到同一 switch，diff 无其它 hunk；既有 case 体零改动 | 只追加（契约 §4:60 已明文授权 `meta.update.set`） |
| 8 | `src/host/meta-store.ts:19-21,34` `AgentMetaDoc` 加**必填**字段 `update` | 既有导出类型加必填字段（TS 层破坏性） | 契约 §4:60 明列 Added-only → 不判违规，但自述须写「必填」 |
| 9 | `src/host/meta-store.ts:56 EMPTY` / `:84 snapshot()` / `:100 load()` / `:209-221` 新方法 | snapshot 使既有 `meta.get` 载荷多一个 `update` 字段 | 追加（同上授权）；`updatePrefs` 同步、`setUpdate` 非法值静默（与 `setPreset` 同风格） |
| 10 | `tools/verify/host-entry.ts:23`（import 行改写）+ `:41-43`（给临时宿主目录挂 `node_modules` junction） | **共享验证器夹具被改**，非「注册点」：既有全部断言的解析环境因此变化 | **轻度越线**（§8⑧ 只许加注册点/分区）→ 见整改项 1 |
| 11 | `package.json` verify 链末追加 `update-host.ts` + `dependencies` 新增 exact | 链上只加 1 项；依赖块为新增 | 只追加 |
| 12 | `package-lock.json`（+13 行，root deps + 包条目） | npm 安装产物 | 只追加；**T7 自述清单未列** → 补记 |

**结论：未发现「修改既有 case 行为」「删改/重排他人追加」「反向依赖」**。字面非只追加共 2 处（#6 签名替换、#10 共享验证器夹具），均兼容/环境级，无行为回归。`prototypes/update-center/`（mtime 15:54:35，落在 T7 写入窗口内）未列入 T7 清单，归属待澄清。

## 违反契约条目（如有）

1. **命名偏离 §3**（低危·文档面）：§3 定 `im-companion.<feature>.<action>`，T7 的 3 端点实为 `imc.*`（`rpc.ts:176-178`）。豁免理由（包 `buildPhoneNames` 物理上拼不出点号）只写在 `CONTEXT.md` Boundaries（未提交），`docs/features-contract.md` §3/§4 未留一行判据 → 按 §9「契约变更先行」应补记。
2. **§8⑧ 共享验证器**（低危）：`host-entry.ts:41-43` 是夹具改造而非注册点（见上表 #10）。
3. 未违反项（逐条核对通过）：§3 载体条款——自有桥**只**经 `ctx.connection.fetch.register({ path: '/api/im-companion' })`（`index.ts:32`），全仓无 `connection.rpc.handle` 调用（`index.ts:10` 仅注释）；3 个端点确实落在本仓端点表 `host/rpc.ts` 的 switch 内；§1 目录形态（host 侧模块沿用既有 `src/host/*` 惯例，未侵入 `src/features/**`）；§5 红线；§10 三件套（清单/自述/可移除性）。

## 越界检查

- `src/client/**`：T7 零改动（`git status` 无 client 条目）。**注意**：审查窗口内 `src/client/components/panel.ts` 出现改动（mtime 16:05:34，diff 自述「T6（#89）更新中心挂载点」），系**并发 agent 所写**，非 T7；本审查所有写操作均在 `%TEMP%` 副本内，live 树零写入。
- `docs/**`、`CONTEXT.md`：有未提交改动，但内容归属 T4 #87（mtime 15:41-15:42，早于 T7 源码 15:49+），非 T7 触碰。
- A1 私有文件（`panel.ts` / `connect-flow.ts` / `row-actions.ts`）：T7 未触碰。
- 端点落点复核：`imc.*` 三 case 在 `src/host/rpc.ts` switch 内（非别处），由 `index.ts:49` 的 `handler(call.method, …)` 唯一派发。

## 可移除性验证结果（含前后 git status 对照）

**做了。方法（等效法，零触碰被审树）**：robocopy 全量副本到 `%TEMP%`（排除 `node_modules`，用 junction 指回本仓）→ 副本内 `git apply -R` 反打 5 个修改文件的 T7 补丁 + 删 6 个新增文件 + `git checkout -- package-lock.json` → 在副本跑 `npm run check`。

- 副本「T7 已摘净」佐证：`git diff --stat -- <5 个文件>` 输出为空；副本 `git status` = live 减 T7 的 8 处（M 里去掉 package.json/rpc.ts/meta-store.ts/index.ts/host-entry.ts/package-lock.json，?? 里去掉 6 个新增文件）。
- 结果：`guard: 101 files, max 300 lines, limit 300 — PASS`，`[CHECK-EXIT=0]`（build + typecheck + 17 项 verify + guard 全绿，101 = live 106 − T7 的 5 个 src 文件）。
- **前后 git status 对照**：操作前 live = 8 M（CONTEXT.md / docs/features-contract.md / package-lock.json / package.json / meta-store.ts / rpc.ts / index.ts / host-entry.ts）+ 18 `??`；操作后 live = 同上 **+ ` M src/client/components/panel.ts`**（并发 T6 #89，非本审查产物）。我的操作未改 live 任何字节：`lib/` 前后哈希逐一相同（`client.js E9104B58…`、`index.js 49425B8C…`，mtime 16:04:39 未变）。
- 副本已删除（junction 先 `rmdir` 摘除再删目录，防递归误删 live `node_modules`；已确认 `node_modules/dsh-plugin-update` intact）。
- 结论：**可移除性通过**——T7 整体移开后 `npm run check` 仍全绿，O(N) 证明成立（无隐藏耦合残留；前提是 `host-entry.ts` 的 junction 与 `package.json` 两处变更随 T7 一起回退，二者已含在补丁内）。

## 红线与依赖核验

- `npm run guard`（live，含 T7）：`guard: 106 files, max 300 lines, limit 300 — PASS`，**exit 0**。
- 真实行数（node `split(/\r?\n/)` 含末尾空项；括号内为 guard 口径）：`update.ts 237 (236)`、`update-reader.ts 194 (193)`、`update-schedule.ts 165 (164)`、`update-paths.ts 92 (91)`、`dsh-plugin-update.d.ts 35 (34)`、`rpc.ts 193`、`meta-store.ts 268`、`index.ts 78`；`tools/verify/features/update-host.ts 397`（tools/** 豁免）。无 BOM、LF 换行。全部 ≤300。
- 依赖落点：`package.json` → `"dependencies": { "dsh-plugin-update": "0.1.1" }`，**exact（无 ^/~）**；`node_modules/dsh-plugin-update@0.1.1` 实装（`exports: "." → ./dist/host.js`）；`package-lock.json` root deps 同步 exact、resolve 走 npmmirror（与既有全部条目同 host）；`devDependencies` 未动（diff 只有新增 dependencies 块）。
- 功能证据：`tsc -p tsconfig.json --noEmit` exit 0；`node --test tools/verify/features/update-host.ts` → `tests 10 / pass 10 / fail 0`，exit 0。

## 结构判断

一句判断：**拆分合理、方向单向、无循环**——`update-paths.ts`（同步路径/版本，零本仓依赖）← `update-reader.ts`（环境读取 + 指纹，仅依赖包的 2 个导出）← `update-schedule.ts`（纯定时器状态机，只依赖 `UpdatePrefs` 类型）← `update.ts`（接线 + 信封 + autoCheck，唯一出口）；`rpc.ts` 只声明端口接口、不 import update 模块，依赖反向正确；`index.ts` 只做装配。没有把无关东西塞在一起，也没有把内聚的东西拆散。
唯一瑕疵：`update.ts:52` `UpdatePrefsStore.setUpdate(input: { autoCheck?: … })` 声明的字段名是 `autoCheck`，与 `meta-store.ts:15-18` 明定的 `autoCheckEnabled` 不一致，且该成员全仓无人调用（方法双变体使 typecheck 放行，属潜在陷阱）。

## 评分与裁决

- **解耦合规评分：88 / 100**（只追加为主 + 端口倒置干净 + 可移除性通过；扣分给共享验证器夹具触碰、契约判据缺行、自述清单缺项）
- **触碰面风险等级：低**
- **裁决：需整改后合并**（整改项全部为文档/清单/清理级，不动 T7 代码逻辑）
- 整改项（按严重度排序）：
  1. 【中】`host-entry.ts:41-43` 的 junction：要么在契约 §8⑧ 记为「必要的夹具追加」，要么把该需求收进 T7 自己的验证器（`update-host.ts:26` 已有同款，host-entry 可只保留对本仓产物零副作用的断言）。
  2. 【中】把 `imc.*` 电话名对 §3 命名法的现实例外从 `CONTEXT.md` 搬进 `docs/features-contract.md`（§9 契约先行，否则后续评审无判据）。
  3. 【中】补自述清单：`package-lock.json`(+13) 与 `prototypes/update-center/`（mtime 15:54 落在 T7 窗口内）须明示归属。
  4. 【低】`update.ts:52` 的 `autoCheck` 字段名对齐 `autoCheckEnabled`（或删掉该死成员）。
  5. 【低】`rpc.ts:56` 的既有导出签名替换须在「触碰面自述」里单列，现清单只有「修改 rpc.ts」这一句。
