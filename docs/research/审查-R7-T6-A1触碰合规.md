# 审查 R7：T6（#89）"先合并点：panel.ts 更新区挂载点" —— A1 私有文件触碰合规

审查基线：`HEAD=875c8ac`（工作树，改动**未提交**）。被审改动仅两个文件，命令
`git diff --numstat -- src/client/components/panel.ts src/client/data/config.ts` → `17 2` / `4 0`（净 +15 / +4）。
只读审查：除本报告外未写任何仓库文件，未做 stash/checkout/worktree，未动 GitHub。

## 两处改既有行的裁定

改动点共 3 处落在既有行上视野内：① `panel.ts:8` import 行；② `panel.ts:178` `root` 行；③ `panel.ts:215` `__afDispose` 体内新增一条语句。

- ① import 行（`panel.ts:8`）：仅在既有具名导入列表里追加 `UPDATE_CENTER_HOST_EVENT`，其余 ADOPT_VIEW_EVENT / FLEET_VIEW_EVENT / type FleetViewDetail 一字未动，无值变更 → **等价于追加**。
- ② `root` 行（`panel.ts:178`）：既有 5 个子元素的**顺序、类名、行为全部未变**——`hd → toolbar → compose.el → body → promo`，`promo` 仍在末位；新 div 插在 `body` 与 `promo` 之间（`panel.ts:177`）。DOM 结构影响裁定 = **无**：
  - 新元素 `<div id="imc-update-center" class="uc-host">` 在**全仓无任何 CSS**（`af-body|af-promo|uc-host` 全 src grep：`uc-host` 零命中；`panel.ts:169/171` 的 `.af-body` 本身也无规则）；
  - `.af-root`（`theme.ts:24-38`）无 `display:flex/grid`、无 `gap`，纯块流；空块盒高 0、行为零；
  - `promo` 的上间距来自 `first-view-styles.ts:36` 的 `.af-promo{margin-top:12px}`。空 div 是自塌陷块（无高度/内边距/边框/内容），相邻 margin 可穿过它折叠，结果仍是 `max(0,12px)=12px` → **视觉零变化**。
  - 唯一前提：T8 日后给 `.uc-host` 加 margin 才会改动该折叠链（属 T8 责任面，非本次）。
- ③ `__afDispose`（`panel.ts:214-215`）：新增的 `emitUpdateCenterHost(null)` 是**追加一条语句**，不是改写既有语句；派发函数自带 try/catch（`panel.ts:241-247`），不新增抛出面；顺序为"先播 null 再 `data.dispose()`"，方向正确。

**裁定：两处改既有行均属"等价于追加"，未改变既有行为、顺序、类名与间距。**

## 通道合规裁定（window 事件 vs slots）

先例核实（A1↔feature 用 `window` CustomEvent 的**既成**通道，共 6 个常量）：

| 事件常量 | 声明处 | 派发处 | 监听处 |
|---|---|---|---|
| `OPEN_DRAWER_EVENT` | `config.ts:22` | `agent-row.ts:145` | `detail-drawer/drawer.ts:32` |
| `FLEET_VIEW_EVENT` | `config.ts:29` | `panel.ts:252` | `fleet-radar/view.ts:230` |
| `ADOPT_VIEW_EVENT` | `config.ts:32` | `panel.ts:261` | `adopt/view.ts:126` |
| `OPEN_AGENT_EVENT` | `bindings.ts:11` | — | — |
| `SEND_TEST_EVENT` / `SESSION_VIEWED_EVENT` | `header-overlay.ts:12 / :15` | `session-header/panel.ts:31` | — |

- **先例成立**：`window` 事件是既成通道，config.ts 注释亦自陈"FLEET_VIEW_EVENT 同款事件制"。但它**不在**契约 §3/军规③ 的 rpc/subscribe/meta/slots 白名单里 → 这是**契约文本与代码事实的长期漂移，非 T6 首创**。
- **T6 的新语义（无先例）**：既有 6 个事件的 detail 全是 `undefined` 或纯数据（key / view / sessionId）；T6 首次让事件通道承载**活的 DOM 元素**（A1 私有 DOM 的子树所有权）。这更接近 §2 `slots` 的本职（挂载面）；但 `SlotTarget`（`protocol.ts:6`）是闭集 `settings.section | workspace-rail | session-header | conversation-session`，**没有"A1 面板内部"这个 target**——走 slots 须改 `protocol.ts` + 装配层，是更大的契约变更，且非 T6 单独可决。
- **裁定**：不判违规，判"**既成通道 + 新语义**"。须由 F0 决：(a) 在 §3 增列事件制通道并写明"可携带 DOM 挂载点"（记录既成事实，不追溯定罪），或 (b) 新增 `SlotTarget` 把挂载点收进 slots。**两者都不该由 T6 顺手改契约**（§9 契约先行）。
- **隐式共享是否变多：是，但受控。** 新增一条隐式契约（事件名 + `{host}` 形状），仅读 config.ts 注释或契约可知。缓解证据：常量在共享层 `config.ts:36` 导出 → T8 只需 `import { UPDATE_CENTER_HOST_EVENT } from '../../client/data/config'`（与 `adopt/view.ts:5`、`fleet-radar/view.ts:9`、`detail-drawer/data.ts:6` 同款），**不需要 import panel.ts** → 反向依赖红线不破。
- **缺口**：detail 形状是内联匿名（`{ host }`），未像 `OpenDrawerDetail`（`config.ts:24`）/`FleetViewDetail`（`config.ts:38`）那样导出接口 → T8 只能猜形状。

## 零反向依赖核实

- 全仓 grep `components/panel`：唯一命中 `src/client/index.ts:5`（A1 自己的装配层，非 feature）。features 内两处 `./panel`（`adopt/view.ts:12`、`session-header/view.ts:8`）指向**各自同名兄弟文件**（`src/features/adopt/panel.ts`、`src/features/session-header/panel.ts`），**不是** A1 私有文件。
- `panel.ts` 的 import（L4-L20）全为 `../dom`、`../data/*`、`../icons`、`../ui/*`、`./agent-row` 等 client 内部路径，**零** `src/features/**` 引用。
- **双向零反向依赖成立。** `UPDATE_CENTER_HOST_EVENT` 全仓仅 3 处字面引用（`panel.ts:8/244`、`config.ts:36`）+ 1 处类名/id，无第二消费者 → 可移除面收敛。

## 红线与可移除性

- `npm run guard` 原始输出末行：`guard: 106 files, max 300 lines, limit 300 — PASS`，`GUARD_EXIT=0`。
- **guard 口径行数（现树，含注释全行）**：`panel.ts` **268**、`config.ts` **67**（均 <300，panel.ts 余量 32 行）。Node 复核：panel.ts 12695B / 268 行、config.ts 2893B / 66 LF，**两端无 BOM**（未用 PowerShell `Get-Content|.Count`，其在本仓会少算）。
- **可移除性：已做，是真跑（非推演）。** 方法：`git clone --no-hardlinks` 到 `%TEMP%\imc-r7-copy`（`node_modules` 用 Junction 回本仓）→ 副本 `git apply` 正向打补丁（`apply exit=0`，副本 numstat 复现 `17 2` / `4 0`）→ 副本 `npm run check` → 副本 `git apply -R`（`reverse exit=0`，副本 diff 清空）→ 副本 `npm run check`。
  - 反向（apply -R 后）：`guard: 101 files … PASS`、panel.ts **253** / config.ts **63**、**`CHECK_WITHOUT_EXIT=0`** → 移除后全绿。
  - 正向（含改动）：同一条 `npm run check` 走到 && 链最后一环 guard PASS（该次日志中 config.ts=67 即含改动）；guard 能到达即证明 build+typecheck+verify 均已通过。
  - **活树零写入证据**：改动两文件 SHA256 前后一致（panel.ts `7D8A376D…A93D72`、config.ts `E50D1E4E…166C75`），`git status --porcelain` 前后 `identical=True`；补丁经 `git diff --output=` 只落副本；未执行任何 stash/checkout/worktree。
  - 附带结论：克隆基线为 HEAD（不含未提交改动），**正向 apply 干净成功**本身即证明"现树改动 = HEAD + 本补丁"，无隐藏上下文依赖。
- **附带证据（自述数字）**：自述"+19/-2"来自 `git diff --stat`（计**变动行** 17+2=19），真实 `--numstat` 为 `17 2`（净 +15）；`config.ts` 的 `4 0` 无误。

## 先行入库状态

- **未入库。** `git status --porcelain`：`src/client/components/panel.ts` 与 `src/client/data/config.ts` 均为 ` M`（已改未提交），HEAD 仍是 `875c8ac`。故本票自称的"先合并点单 PR 先行入库"**目前只是工作树上的未提交改动**，§9 与 §7「每线开工前先合入先合并点」**未满足**。
- 真实影响：① 工作树同时混着 T4 的 host 改动（`src/host/update*.ts`、`tools/verify/features/update-host.ts`、`src/index.ts`、`package.json` 等）与多份 R1–R6 报告 → 不筛文件的 PR 会把无关 WIP 一并带入；② T8 若依赖它，就是依赖"未提交状态"，任何 checkout/合并都可能把它抽走，且无 commit hash 可锚定；③ 并行开工的基线不确定，冲突面扩大。
- 建议：把这两个文件做成**单独一个 commit** 入库（入库前 `git pull`，按 §9），不得夹带 T4 文件；未入库则 T8 必须停等。

## 评分与结论

- **A1 触碰合规评分：78 / 100**
- **触碰面风险等级：中**
- **裁决：需整改后合并**
- 整改项（按严重度排序）：
  1. **【高】先合并点未成立**：仅提交 `panel.ts` + `config.ts` 两文件的独立 commit 入库（先 `git pull`），禁止夹带 T4 的 host 文件；未入库前 T8 不得开工。
  2. **【中】契约补白（F0 决，不得 T6 自改）**：`window` 事件不在 ③ 白名单，而 T6 首次让事件通道承载 DOM 元素。请 F0 二选一落文档：(a) §3 增列事件制通道并写明可携带 DOM 挂载点；(b) 新增 `SlotTarget` 把挂载点收进 slots 正规通道，T6 改走 slots。
  3. **【中】导出 detail 类型**：`config.ts` 追加 `export interface UpdateCenterHostDetail { host: HTMLElement | null }`（纯新增，合 Added-only），派发处显式泛型化，消除 T8 猜形状。
  4. **【中】无重放/缓冲**：仅在面板构造时广播一次（`panel.ts:180`）。按 AGENTS.md 首选验证流（重打 lib/ → 热重载插件/刷新），若 T8 在广播之后挂载将静默拿不到 host。须补"拿到即再播"或明确在常量注释写"迟挂载者须自取一次"。
  5. **【低】命名空间与多余 id**：`class="uc-host"` 不符军规⑥ `<feature>-*` 前缀（宜 `update-center-host`），且已写进 A1 私有文件 → 后改要再动 A1（又一次单 PR）；`id="imc-update-center"` 全仓零引用，且会成为绕过事件的第二查找通道（`getElementById`），容器已随 `detail.host` 交付，建议删除。
  6. **【低】自述数字修正**：触碰面自述"+19/-2"应更正为 `panel.ts` +17/-2（净 +15）、`config.ts` +4/-0；§10 依赖自述准确。
