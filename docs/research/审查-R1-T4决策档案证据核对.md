# 审查 R1 · `更新系统-T4决策档案` 证据核对（对抗式，只读）

> 权威基准＝包真身 `C:\Users\辰辰洋洋\AppData\Local\Temp\dsh-plugin-update-probe\node_modules\dsh-plugin-update\`（v0.1.1，`package.json` 实读）。
> **基准错位（本次最重要发现）**：包真身**只发 `dist/*.js`**（另 `package.json`/`README.md`/`LICENSE` 等），**没有任何 `.ts`**；档案全部 `xxx.ts:line` 引用在包真身里指向不存在的文件。这些行号在**另一棵树**——`D:\dsh-plugin\dsh-mattpocock-skills-deck\packages\dsh-plugin-update\src\`（同版本 0.1.1，`package.json` 实读）——中**逐条精确命中**。故本表按「内容是否真」与「引用基准是否可用」两个维度分别判定。

## 基准对照（同一行代码，两棵树）

| 内容 | 档案引用（deck src 树，命中） | 包真身 dist 实际行 |
|---|---|---|
| `if (checking) return checking` | `service.ts:328` | `dist/service.js:236` |
| `update-busy` ×4 | `service.ts:404/411/420/426` | `dist/service.js:304/311/320/326` |
| 2 s 复用条件 / `checkId` 置空 | `service.ts:330` / `:352` | `dist/service.js:237` / `:258` |
| `RECHECK_WINDOW_MS = 2_000` | `config.ts:37`（`service.ts:31` 亦重复定义） | `dist/config.js:15`（`dist/service.js:6`） |
| 六字段快照 / 8 枚举值 | `ports.ts:44-57` / `:16-24` | **不存在**（`dist/ports.js` 仅 5 行，只导出 `PORTS_SOURCE`） |
| `panelPollMs` 声明+校验 | `config.ts:60,73,130-134` | `dist/config.js:59-63`（+ 类型无 dist 对应） |
| `setTimeout`（子进程超时） | `store.ts:337-339` | `dist/store.js:249` |
| `status()` 只读本地 | `service.ts:321-325` | `dist/service.js:230-234` |
| `manual` 回包 | `host.ts:307` | `dist/host.js` 同构（成功分支） |

## 逐条核对表

| # | 断言 | 判定 | 实际证据（命令/行内容） |
|---|---|---|---|
| 1 | `service.ts:328` = `if (checking) return checking` | 部分真 | 该行逐字命中，但是 **deck src**；包真身无 `service.ts`，对应 `dist/service.js:236` |
| 2 | `update-busy` 只在 `install()` 抛，`service.ts:404/411/420/426`；检查路径不抛 | 部分真 | 4 处**全在 `install()`**（src:404/411/420/426＝dist:304/311/320/326）；`check()/status()` 零命中 ✅ 事实成立，行号为 src 树 |
| 3 | `RECHECK_WINDOW_MS=2_000`@`config.ts:37`；2 s 复用@`service.ts:330`；失败清空 `checkId`@`:352` | 部分真 | 三条内容全真（src:37/330/352；dist:config.js:15 / service.js:237 / :258 `checked={...checkId:null,expiresAt:0}`）；行号为 src 树 |
| 4 | 包内无任何轮询：`setInterval` 零命中；`setTimeout` 只在 `store.ts:337-339`；`panelPollMs` 无消费者 | **真** | 见下方「第 4 条复核命令」。最要紧的一条**成立** |
| 5 | `ports.ts:44-57` 恰好六字段；`ports.ts:16-24` 八个枚举值 | 部分真 | 内容真（六字段 runningVersion/installedVersion/latestVersion/canInstall/blockedReason/job；八枚举逐字见下节）；位置在 src 树，包真身 `dist/ports.js` 是空壳 |
| 6 | `commands.ts:105` → `null`；第二原因 `commands.ts:118-120` | 部分真 | `commands.ts:105` 逐字命中（`sourceInstall \|\| blockedReason==='source-install' \|\| 'unknown-profile'`）✅；**但「命令为空的第二种原因」实际在 `commands.ts:106-107`**（`usableProfileName` 判非法→`null`），`118-120` 是排序收尾行 → 错引 |
| 7 | `reader.ts:231,233` → `source-install`→`blockedReason`→`eligible=false`；`service.ts:301-302` 需 `env.eligible` | 部分真 | 内容全真（src:231 `else if (result.sourceInstall) result.blockedReason='source-install'`；:233 `result.eligible=!result.blockedReason`；service:301-302 `canInstall=Boolean(env.eligible && ...)`），行号为 src 树 |
| 8 | `service.ts:295-303`：`pending-restart` 时 `canInstall` 必假 | 部分真 | 真（:295-296 置 `pending-restart`，:303 是 `!blockedReason`）；行号为 src 树 |
| 9 | `service.ts:321-325`：`status()` 只读本地不联网 | 部分真 | 真（`ports.readInstalled()`+`loadJob()`，无 fetch）；旁证 `ports.ts:199` 注释「查状态：只读本地，不联网」✅；行号为 src 树 |
| 10 | `host.ts:307` 每次回包都带 `manual` | 部分真 | :307 成功回包确有 `manual: … ?? null` ✅；**失败回包 `host.ts:312` 为 `{ok:false,...payload}`，无 `manual`** ⇒「每次回包」过强（真身 README 第 222 行同措辞，同病） |
| 11 | README §8「直接展示『用户该做什么』那一列」确实存在 | **真** | 真身 README 第 189 行 `## 8. 八种装不了原因（blockedReason 对照表）`；第 194-203 行表；**第 205 行**逐字：「面板拿到非空原因时，直接展示上表"用户该做什么"那一列的一句话，不要只展示英文原因本身。」 |
| 12 | 八种中文文案表 vs README §8 原表 | 部分真 | 8 项**无漏项、无编造**，给/不给命令列与第 210 行一致（仅 source-install、unknown-profile 为空）；唯 `recovery-required` 语义错配（见错引清单 #2） |
| 13 | 本仓引用：契约 `:56/:47/:44`、`src/index.ts:49`、`:50-58`、`src/client/index.ts:82-89`、`CONTEXT.md:10/12/22` | **真** | 逐条命中：契约:56「connection-stream 是唯一轮询源（15s）」✅；:47「host/rpc.ts 只追加 case；端点命名 `im-companion.<feature>.<action>`」✅；:44「只加导出…禁止改既有签名与行为」✅；index.ts:49 `ctx.effect(() => () => dispose(), …)`✅、:50-58 `already registered` 让位 `return` ✅；client/index.ts:82-89 `return () => {…}` cleanup ✅；CONTEXT 10 Workspace / 12 Channel / 22 Contract（含端点命名法）✅ |
| 14 | 另一仓：`updateFromPackage.js:53-56`、`SettingsPage.js:175,190,195,197,225-230` | 部分真 | `:53-56` 恰为 `phoneNames` + 三 handler ✅ 且该文件 `setInterval/setTimeout` **零命中** ✅；`:190/195/197` 三处 `flash(...,'cfg.updateCheckFail','warn')` ✅；`:225-230` 「挂载读一次 + 仅 installing/verifying 期 `setInterval(UPD_POLL)` + cleanup `clearInterval`」逐行 ✅；**`:175` 是 `if (updChecking \|\| updBusy) return` 早退守卫**，真正 `disabled:` 在 `:266`（先例性质对、行号指错） |

## 8 个 blockedReason 逐字对照

`ports.ts:16-24` 逐字（8 值，顺序与档案表一致）：`unknown-profile`、`source-install`、`invalid-installation`、`installation-changed`、`pending-restart`、`registry-conflict`、`incompatible-node`、`recovery-required`。
与真身 README 第 194-203 行「用户该做什么」列对照：`unknown-profile`（修范围名/目录、不给命令）✅、`source-install`（不给命令、按版本号重装）✅、`invalid-installation`（重装当前版本）✅、`installation-changed`（重开宿主再查、仍现则重装）✅、`pending-restart`（重启宿主、非失败）✅、`registry-conflict`（改清单里那一行为版本号）✅、`incompatible-node`（升级 Node 到 22+）✅、`recovery-required`（README：重新点一次安装；**一直出现就按第 12 节排错**）→ 档案写成「一直出现就用下面的命令重装」**✗ 错配**。
**记账（本仓）**：`docs/adr/0001-truth-flags-p1-p3.md:1` 标题含「内容真值 flags」✅、`CONTEXT.md` 全文「真值」零命中 ✅。

## 第 4 条复核命令与结果（最要紧的一条）

```powershell
$d="…\dsh-plugin-update-probe\node_modules\dsh-plugin-update\dist"
Select-String -Path "$d\*.js" -Pattern 'setInterval'   # → ZERO MATCHES（9 个 dist 文件全扫）
Select-String -Path "$d\*.js" -Pattern 'setTimeout'    # → dist/store.js:249（安装子进程超时，唯一）
Select-String -Path "$d\*.js" -Pattern 'panelPollMs'   # → dist/config.js:59,60,61,63（声明+校验，无消费者）
Select-String -Path "<deck>\packages\dsh-plugin-update\src\*.ts" -Pattern 'setInterval|setTimeout'
#   → store.ts:337（类型）、store.ts:339（调用）、node-ambient.d.ts:55（声明）；setInterval 仍为零
```
**裁定：真**。包只给口径（README 第 73 行示例 `UPD_POLL = 1000`、第 79 行面板自写 `setInterval(readStatus, UPD_POLL)`，第 75 行注明「面板里这样用」）；`dist/client.js` 只导出常量与 `assertPollInterval`，无循环。票面「包自带 status 轮询」确被推翻。

## 错引与失真清单

1. **（阻塞级）引用基准错位**：10 条包内断言给出包真身**不存在**的 `.ts` 路径；照档案复核者会在权威源里找不到证据。修正即：改标 `dist/*.js` 行号，或显式写明「行号基准＝deck 源码树 `packages/dsh-plugin-update/src`（同版本 0.1.1）」。
2. **（阻塞级）`recovery-required` 文案错配**：README 第 203 行给的是「按第 12 节排错」，档案改成「用下面的命令重装」——用户可见指令被改写。
3. `host.ts:307`「每次回包都带 `manual`」：失败回包（`host.ts:312`）不带。
4. `commands.ts:118-120` 错引，实为 `commands.ts:106-107`。
5. README「当前版本 `0.1.0`」只在**第 16 行**（§1）；档案写「第 16、38 行」，第 38 行无版本号，且不在 §3。
6. `SettingsPage.js:175` 指为「按钮在途禁用先例」，该行是早退守卫；`disabled:` 在 `:266`。
7. 术语节自相矛盾：正文写「既有 **13** 条一行不改」（`CONTEXT.md` Glossary 实为 **16** 行）、又说「只加 5 个新词」（所给文本块与文末清单均为 **6** 行）。
8. **未证实/无法核实**（不计失真）：`fiber.ts:403-413,676-681`（DSH 内部，未展开）；T1/T2/T3 兄弟档案的 4 处交叉引用已抽查命中（T1 §e:94-95 释放函数 ✅、T2 §a:52 `dsh-plugin-update@0.1.1` ✅、T2 §d「点分名直接透传」✅、T3 §2.3:154 正则 ✅），其余 T3 §4.1/§4.2/§4.3、T1 §b 未逐条展开。

## README 快照可用性裁定

- **双重编码属实**：`（`（U+FF08，UTF-8 `EF BC 88`）在快照里是 `C3 AF C2 BC C2 88`，即「UTF-8 字节→Latin-1 字符→再 UTF-8」逐字节双编；33898 字节 vs 真身 19903 字节。
- **但「行号不可信」不成立**：快照与真身 README **同为 290 行**（LF 计数），逐行比对 ASCII 骨架差异 **0 行** ⇒ 行号 1:1 对齐；抽查档案所引 33 个行号（16/38/79/120/133/194-203/205/209/210/215-218/222/226-233/265）在真身 README 上逐条对得上（唯一不符是第 38 行，见错引 #5）。
- **可用，但建议换源**：`.tmp-wayfinder/pkg-readme.md` 与包真身 README **SHA256 完全相同**（`2FF19615BDB32EC377841A29741AC1A0E807076B03E7DEEA25E8DBBC9B1AEAE5`），后续一律以它为证；乱码快照只可当作行号索引。

## 结论

**证据保真度：78 / 100**（事实层强、引用层弱：14 条断言中 3 条完全真、11 条「内容真＋引用基准错位或小错引」；核心第 4 条成立）。
**阻塞级缺陷：2 条**（上列 #1 #2）。非阻塞失真 5 条（#3–#7）。
**裁决：需修正后使用**——先修引用基准与 `recovery-required` 文案，再交 T7/T8；不必推翻其结论，但不得按现引用直接复核。
**未修改被审文件**；本报告为唯一交付物。
