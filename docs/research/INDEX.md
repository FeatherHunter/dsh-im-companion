# `docs/research/` 索引（更新系统 effort T1–T10）

> **目的**：把「这份东西现在还算不算数」的判断，**从读者的检索成本变成作者的维护成本**——下一个人（或下一个 session）不必逐份读完这里全部报告（编制时 31 份，不含本页），才知道哪些结论已过期、被谁取代。
>
> **读法**：先读本页的「是否仍有效」列，再决定要不要读原文与读到哪一节。**本页与原文冲突时，以本页指向的「取代者」为准。**
>
> **维护规矩**：新增或修订 `docs/research/` 里的文档时，**同一次改动里**更新本页对应行（一文件一行，结论 ≤1 句，不复制细节）。
>
> 编制：2026-09-12；由 `docs/research/审查-ADV2-文档第一性原理.md` 的整改项 1 触发。状态只用四个值：**有效 / 部分失效 / 已被取代 / 未判定**。

---

## A. 本 effort（更新系统）：取证与档案

| 文件 | 一句话结论 | 是否仍有效 | 被谁取代 / 备注 |
|---|---|---|---|
| `更新系统-宿主接线取证.md`（T1 · #84） | `ctx.get`/`ctx.inject` 成立；安装路由服务名 `desktopPnpm` / `subprocess`；自动检查一律走受管 `ctx.interval`；`dsh-plugin-update` 必须装在本仓 `node_modules` 才能被解析（realpath 链不含 profile 的 `node_modules`） | **部分失效** | 仅「`desktopProfiles` 注册方未定位、推断由 Desktop 主进程注册」一项已被取代 → `排查-注入类缺陷穷尽清单.md:52`（asar 原文坐实）+ `探查-T10安装路径前置.md` Q1（四路证据）。其余硬约束仍有效 |
| `更新系统-客户端派生与构建取证.md`（T2 · #85） | 派生工具实跑成功（111 行、ESM、零 import、5 个 `UPD_*` 常量）；点分端点名可直接用、无需翻译层；client 源码禁 `import 'dsh-plugin-update'` | **有效** | 附注：接入形态最终由 T8 落成「直接冻结字面量」（非本文的 4 方案之一），见 `审查-R10-…md`。硬约束「`npm run check` 任何机器可跑」仍有效 |
| `更新系统-版本说明源取证.md`（T3 · #86） | 源可用性实测：首选 raw `master/CHANGELOG.md`、备选 jsDelivr `@master`、`github.com/.../raw/` **禁用**；切节必须锚行首 `^## v<semver> — <日期>`；离线与 CORS 失败在 JS 层同形 | **有效** | 本 effort「版本说明源」的**单点真相**（§1.5 含实测数据）；T4 §E.1 应改为指针指向本文 |
| `更新系统-更新包真身核查.md` | 包 `dsh-plugin-update@0.1.1` 真身事实表（只发 `dist/`、零运行时依赖、单入口）+ 8 种 `blockedReason` 取值表 + 电话名 handler 键 = `imc.update*` | **有效** | 包真身与包枚举的**单点真相**；引用包内行号一律以本文（dist 产物）为准，不得混用上游 `.ts` 源行号 |
| `更新系统-T4决策档案.md`（T4 · #87） | 本 effort **唯一决策档案**：owner 三条裁定 + §B 中文文案表 + §C 对话框/横幅优先级 + §G 设置项形状 + §H 更新包集成硬约束 | **部分失效** | ①§E.1 源优先级与现码不符（写「第三源 `api.github.com` 可用」，实现 `constants.ts:16-19` 只有 2 源）；②自报 244 行实为 243 行；③其 `.ts` 行号须先对账（见 R1）。其余裁定仍是权威 |
| `更新系统-交付风险与真机路径核查.md` | 对抗式交付风险预演（发布链路 / 预览 harness / 工作树卫生 / 真机路径） | **部分失效** | 正文多数风险结论已被 `审查-R3`（证伪 2 条、坐实并改写 1 条）与 `审查-R4`（机制认错）推翻，**顶部已加失效说明块**；`## 回滚手册` A/B/C **仍有效且唯一可照做** |
| `更新系统-baseline体检.md` | 开工基线：`npm run check` 绿（exit 0）、`lib/` 重建 26/26 逐字节不变、`theme.ts` 300/300 | **部分失效** | 「verify 17 段 / 278 pass / 101 files」是旧态（现以 `package.json` 为准）；26/26 哈希基准与「`git status` 干净不能证明 `lib/` 同步」仍有效（真机验收基准） |

## B. 本 effort：审查 R1–R10

| 文件 | 一句话结论 | 是否仍有效 | 被谁取代 / 备注 |
|---|---|---|---|
| `审查-R1-T4决策档案证据核对.md` | **基准错位**：包真身只发 `dist/*.js`，T4 全部 `xxx.ts:line` 引用指向上游源码树；附两树行号对照表 | **有效** | 引用更新包行号前必读本文 |
| `审查-R2-T4决策质量.md` | 对抗式评审 T4 **草稿**：裁决级别错配 3 处、§B 一行产品误导、漏 3 个必须定的决策 + 电话名「豁免」专节 | **已被取代** | 7 条裁决已逐条落入 `更新系统-T4决策档案.md` §3 定稿；保留作「为什么这么定」的理由链，不当规格用 |
| `审查-R3-风险报告核心结论证伪.md` | 证伪/坐实风险报告：`installation-changed`、`unknown-profile` 坐实；「不会自愈成 `pending-restart`」「修复必须改上游包」**证伪**；`readerOverrides` 是现成口子 | **有效** | **不可替代**：L1–L6 读数表 + 证伪链；`readerOverrides` 必需性的唯一证据源（T7 落地依据） |
| `审查-R4-风险报告决策质量.md` | 机制纠正：真正摘 `dsh.profile.bundles` 的是 `dsh plugin add` 的 `reconcilePlugins`（非 Desktop self-heal）；更近的失败是「部分安装」；scratch profile 隔离性不成立 | **有效** | 4 条纠正已被 `更新系统-T4决策档案.md` §5 第 2/5/7 项吸收 |
| `审查-R5-T7解耦与触碰面.md` | T7 触碰面逐条裁定（只追加 9 条 + 「字面非只追加但等价」2 条）；T7 已入库 | **有效** | 提审自述照此表对照 |
| `审查-R6-T7正确性与规格.md` | T7 规格符合性逐条核对（`readerOverrides` 三件套、自有 reader 10 字段、三电话名、`autoCheck` 形状、调度器生命周期） | **有效** | 与包真身逐行对账；含 1 处低危漏洞 + 1 处未断言 |
| `审查-R7-T6-A1触碰合规.md` | T6 对 A1 私有文件的 3 处改既有行判定「等价于追加」；空 div 视觉零变化的证明 | **有效** | T6 已入库 `acdfb1c` |
| `审查-R8-T6挂载契约压测.md` | 挂载契约压测：机制对、时序真，但缺 2 个高位守卫 ＋ 1 条「每次 attach 重拉 status」约束（P0-a/P0-b/P1-c） | **有效** | **不可替代**：T8 施工图的必守约束；R10 已复核 T8 照做 |
| `审查-R9-T8解耦合规.md` | T8 解耦合规 92/100、触碰面低风险、裁决「可合并」+ 3 条非阻塞整改 | **有效** | 被审 T8 至今仍在活树（未提交）；3 条整改是否关闭**未判定**（见 §F） |
| `审查-R10-T8规格符合性.md` | T8 规格符合性逐条核对（A1–H），裁决「**需整改**」（可在整改后进 T9/T10） | **有效** | 其 P0 一项已由 `验证-V3` 覆盖（V3 裁决「P0 已修复」）；点名整改项是否已关**未判定**（见 §F） |

## C. 本 effort：验证 V1–V4

| 文件 | 一句话结论 | 是否仍有效 | 被谁取代 / 备注 |
|---|---|---|---|
| `验证-V1-T7整改复现.md` | 独立复现 T7 整改声称：4 次定向变异证明 3 条装配点断言是**真断言**（非假绿） | **有效** | 读数与变异矩阵仍可用 |
| `验证-V2-共享验证器夹具必要性.md` | 复现「还原 junction 夹具即红」（`ERR_MODULE_NOT_FOUND`）⇒ 夹具必要，已记入契约 §8 | **有效** | 契约 §8 那条追加的依据 |
| `验证-V3-T8桥诚实性与档位白名单.md` | 14 输入 × 两路径 + 端到端 DOM 桩渲染实测：桥失败**绝不**被渲染成「已是最新」；裁决「P0 已修复」（90/100） | **有效** | 有 5 条**未证实项**（真桥超时行为、空串 token 等），见其 §未证实项——当已证使用会出错 |
| `验证-V4-T8断言变异与可移植性.md` | T8 断言变异测试（M1/M2/M3 各自只打红对应断言）+ 「任何机器可跑」核实（副本 16/16 基线） | **有效** | 变异矩阵仍可用 |

## D. 本 effort：T10 前置与文档审查

| 文件 | 一句话结论 | 是否仍有效 | 被谁取代 / 备注 |
|---|---|---|---|
| `排查-注入类缺陷穷尽清单.md` | 注入维度穷尽：`['connection','timer']` 够用、无遗漏；安装路径风险在 profile 匹配与 `add` 拒绝策略（非 inject）；asar 原文坐实 `desktopProfiles` 在 Loader 之前存在 | **有效** | T10 前置；**取代地图 Notes 里「`desktopProfiles` 注册方未定位」的旧推断** |
| `探查-T10安装路径前置.md` | T10 验收起点三条前置：激活范围＝`web`（四路证据）、只读确认可拉版本、盘上依赖已变真目录 | **有效** | 应抄进 #93 票面（见 ADV2 F3） |
| `审查-ADV1-收尾方案对抗.md` | T9/T10 收尾方案对抗：安装态切换是**必要条件、无等价更短路线**；临时 profile 替代被否（验的是另一条安装路由 `runCliProcess`）；回滚 A/B/C 够用但备份面不全 | **有效** | T10 收尾必读；其整改项（自动检查验收条款 / 重启杀会话预算 / 回退步与备份面 `pnpm-lock.yaml`）**是否已落进方案未判定**（见 §F） |
| `审查-ADV2-文档第一性原理.md` | 文档体系第一性原理审查：**68/100**，裁决「需精简 + 局部重构」+ 7 条整改项 | **有效** | 本页即其整改项 1 的产物；其整改项 3（地图）、5（T4 沉降）、7（并文件）**未执行** |

## E. 非本 effort 的历史件（旧 effort，混放在本目录）

| 文件 | 一句话结论 | 是否仍有效 | 被谁取代 / 备注 |
|---|---|---|---|
| `首屏盘点.md`（2026-09-04 · #24/#22） | 首屏 FleetPanel 现状盘点：骨架 / 行数 / 态 / 样式 / 红线 / 禁区 | **部分失效** | 09-04 快照；此后首屏文件已被多票改动（如 T6 加挂载点），行数与结构读数需重盘 |
| `06-slot-coexistence.md`（2026-09-03 · E4） | `conversation.session` 是 single-kind 独占槽 ⇒ 撞车会打挂宿主启动；给出共存规避法 | **有效** | 机制类结论（DSH 槽位语义）仍成立 |
| `05-e4-unread-source.md`（2026-09-03 · E4） | 欢迎横幅「今日 N 会话 / M 未读」的可行来源调研 | **部分失效** | 文件自身第 7 行已注：结论 5 的 B 方案（水位路线）作废（2026-09-08 #41，水位 09-07 退役） |
| `vision-router-screenshot.md`（2026-09-07） | 第三方包 `dsh-vision-router@2.1.3` 截图功能调查（源码级） | **有效** | 基线 `c6e05dc` / `2.1.3` 与本机已装版本一致 |
| `vision-router-efficiency.md`（2026-09-07） | 同一包的效率机制调查（「云视觉调用是唯一稀缺资源」五条主线） | **有效** | 同上，基线 `c6e05dc` / `2.1.3` |
| `vision-router-screenshot-local-patch.md`（2026-09-07） | 本机对已装 `dsh-vision-router` 的截屏补丁记录（上游 issue #409） | **有效** | 补丁对象是已安装包内 `index.js`；该包重装后补丁是否仍在**未判定**（见 §F） |

---

## F. 已知未判定（本页作者没判的，别当成已判）

1. **T8 现状与 R9/R10/ADV1 的整改项关闭情况**：T8 至今仍是活树未提交状态——`审查-R9`（3 条非阻塞整改）、`审查-R10`（裁决「需整改」）、`审查-ADV1`（收尾方案 3 条必须整改项）点名的整改**是否已关闭、读数对应哪次工作树快照，均未判定**。要判需比对 T8 收尾 commit、或重跑这三份审查、或看收尾方案是否已改。
2. **`vision-router-screenshot-local-patch.md` 的补丁是否仍生效**：未核对已装包当前内容。
3. **各报告的「段数 / pass 数 / 文件数」类读数**（baseline 17 段、T4 §I.6、V1、V3、R10 的 19 段等）：一律**已被取代**，唯一权威是 `package.json` 的 `scripts.verify`（当前 19 段）——故不逐条在本页登记。
