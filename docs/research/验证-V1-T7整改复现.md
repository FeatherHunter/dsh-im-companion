# 验证-V1：T7（issue #90）宿主侧整改声称的独立复现

- 验证员 V1（活树）｜仓库 `D:\dsh-plugin\dsh-im-companion`｜HEAD `177f3839`｜Node v24.19.0 / Windows / PowerShell
- 手法：点名文件只读 + 4 次**临时定向变异实验**（备份 → 改 → 跑 → `Copy-Item` 还原 → SHA256 对照）+ 活树跑一次 `npm run check`
- 变异只碰 2 个文件：`src/host/update-schedule.ts`、`src/index.ts`；其余被审文件全程只读
- 对照台与全部夹具建在 `$TEMP\v1-parity\`，仓库内未新建/未修改任何源码或测试文件

## 逐条声称的复现结果（成立/不成立/无法判定 + 证据）

**① `start()` 先 `releaseAll()`：成立（效果已证，不只信注释）**
`update-schedule.ts:136` 确为该分支第一句。变异 M1 只删这一句 ⇒ 唯一相关断言 `T7-6c` 变红（`周期句柄恒一份（不双份）` actual 2 / expected 1，`exit 1`）⇒ 该行是**承重行**，注释所述竞态真实存在且被覆盖。

**② 3 条装配点断言是真断言：成立（见下节变异测试）**
`T7-6a`:448 / `T7-6b`:463 / `T7-6c`:493 位置与描述相符；断言触及真计数器、真路由调用（`response.status===200`、`meta.update.set` 载荷）、真释放记录（`released.includes('interval:86400000#0')`），非装饰。三次定向变异各自**只**打红对应那一条、另两条不受影响 ⇒ **不是「无论如何都会过」的假绿**。

**③ `update.ts` 死字段 `autoCheck?` 已删：成立（以「无残留」为判据，附限制）**
保留的 9 处小写 `autoCheck`（7 行：9/78/91/176/186/187/190）全部是**运行期状态名**（`AutoCheckView`、桥方法 `autoCheck()`、`reply()` 注入键）与注释，加 `autoCheckEnabled`（设置项，走 `meta.get` / `meta.update.set`）。判据：全仓（`src`/`tools`/`prototypes`）**`autoCheck?` 字面量命中 0 行**；三个端口签名 `UpdateHostOptions`、`UpdateReaderOptions`、`UpdatePrefsStore.setUpdate(input: { intervalHours?: unknown })` 均不收 `autoCheck`。
限制：`update.ts` 是未跟踪新文件，git 无「删之前」版本 ⇒ 只能证**现在没有**，「曾经有过」不可复现。

**④ `update-reader.ts` 两处对齐上游且无回归：成立**
(a) 非法 `profileName`：实测 `-web`、`node_modules` 两个非法名，T7 与包真身 `dist/reader.js:117-122` **逐字段相同**——`profileName` 写原名、`blockedReason='unknown-profile'`、`homeDir/profileDir/installedVersion/packageValid/sourceInstall/eligible` 全同。
(b) `entryIsFile` realpath 复判（`update-reader.ts:93-97` vs 上游 `dist/reader.js:57-63`）：语义逐句对应；回归探针见下节 D 例。活树入口 `lib/index.js`、`lib/client.js`、`cordis.patch.yml` 均为普通文件（`LinkTarget` 空，无 reparse point）⇒ 复判在本机不可能误伤。

**⑤ `npm run check` exit 0、verify 18 段全绿、guard PASS：成立**
`exit 0`（17 s，日志 1982 行）。四段全过：build（`tsc -p tsconfig.json` + `tsdown`）/ typecheck（`tsc -p tsconfig.client.json`）/ verify / guard。verify 共 **18 段**（16 段 `node --test` + `render-client.ts` + `session-header-render.ts`），逐段 **16 组 `fail 0`**、`pass` 合计 291、`not ok` **0 行**。guard 原始行：`guard: 106 files, max 300 lines, limit 300 — PASS`。
噪声归属（已核）：`Maximum update depth`（609 行命中）与 `NativeCommandError`（1 行）**全部落在 T7 段之前的 `tools/verify/render-client.ts` 段**，该文件本次未被修改；单独跑 `node --test tools/verify/features/update-host.ts` 时两者均为 **0 行** ⇒ 不是 T7 引入的新噪声。

## 变异测试结果

| # | 临时变异 | 预期 | 实测 |
|---|---|---|---|
| M1 | `update-schedule.ts` `start()` 删 `releaseAll()` | `T7-6c` 红 | ✔ 红：`周期句柄恒一份（不双份）` actual 2 / expected 1；13 测试 12 过 1 败，exit 1 |
| M2 | `update-schedule.ts` `apply()` 删 `releasePeriod()` | `T7-6b` 红 | ✔ 红：`周期句柄恒一份：旧档位那一份必须先释放` actual 2 / expected 1 |
| M3a | `src/index.ts` 让位分支删 `return` | —（作废） | ✘ **无效变异**：catch 末尾 `throw error` 兜住 ⇒ 失败原因是 rethrow 而非断言，**不能**作为 6a 证据 |
| M3b | 改为「让位但不早退」（warn 后继续往下建更新系统） | `T7-6a` 红 | ✔ 红：`让位分支不得留任何在册句柄` actual 2 / expected 0 |
| M4 | （旁证）单独跑 T7 段 | 全绿 | ✔ 13/13 过，exit 0，零噪声 |

结论：三条装配点断言各自**对其实施的缺陷过敏**，均为真断言。M3a 的作废过程一并留档，避免把「因错理由变红」当成证据。

## source-install 无回归的证据

对照台：T7 自有 `readUpdateEnv`（`src/host/update-reader.ts`）vs 包真身 `$TEMP\dsh-plugin-update-probe\node_modules\dsh-plugin-update\dist\reader.js` 的 `readInstalledReal`（`createUpdateReader().readEnv()`），同一夹具同一盘。

| 夹具 | T7 | 上游真身 | 判据 |
|---|---|---|---|
| A `link:` + 目录 junction（= 本机真机形态） | `sourceInstall=true` / `blockedReason='source-install'` | `sourceInstall=true` / `packageValid=true` | **核心读数成立**；除 `blockedReason` 外 9 字段逐字段相同 |
| B `link:` + 普通实装目录 | `'source-install'` | `sourceInstall=true` | 同判 |
| C 版本号 spec `1.2.3` | `sourceInstall=false` / `blockedReason=null` / `eligible=true` | `sourceInstall=false`；`blockedReason='installation-changed'` | 说明符判据同判；上游那一条是 R3 已坐实的既有缺陷（本读取器就是要消掉它） |
| D 入口经 junction 越出包目录（新 realpath 复判的回归探针） | `packageValid=false` → `invalid-installation` | `packageValid=false` | **与上游同判**：复判只补严到上游口径，未引入比上游更严的新门槛 |
| E 非法 profileName（`-web` / `node_modules`） | `unknown-profile`，`profileName` = 原名 | 同 | 逐字段相同 |

A/B/D 例 `packageValid` 均为上游同判、A/B 例在 junction 形态下仍为 `true` ⇒ ④ 的两处改动**不会破坏 `source-install` 判定**，也不会把本机 junction 安装误判成 `invalid-installation`。

## 活树零污染证据

- `src/host/update-schedule.ts`：实验前 `3A2FF3C412B1F82B66B996FF1F38CE1A6CF52BA9CE443223BAC1F3D8B788BCAF`，还原后**逐字节相同**（`Copy-Item` 还原 + SHA256 对照），变异标记残留 0 处
- `src/index.ts`：实验前/后均 `A08409B5A609F6A94EB23B3157C358A77C54DCE3D12CEF9C3800B2A343B676AE`
- `git status --porcelain` 实验前后一致（6 个 ` M`：`package.json` `package-lock.json` `src/index.ts` `src/host/meta-store.ts` `src/host/rpc.ts` `tools/verify/host-entry.ts`；7 个 `??`：`src/host/*` 新文件 + `tools/verify/features/update-host.ts` + `prototypes/update-center/`）。期间多出的 `docs/research/验证-V2-共享验证器夹具必要性.md` 是 V2 的产物，非本次触碰
- `lib/` 全部 38 个文件：`npm run check` 前/后 SHA256 **差异 0 行** ⇒ 我这次构建未改动任何运行态产物（无热重载副作用）
- 对照台脚本与夹具全在 `$TEMP\v1-parity\`；仓库内零新建、零修改（临时变异已逐字节还原）

## 结论

- **声称可信度：93 / 100**（扣分项：③ 的「已删」在未跟踪文件上只能证 absence；verify 与实现同作者、无独立 oracle——已用变异测试补强）
- **假绿风险等级：低**（三条装配点断言全部通过定向变异，且 A 例核心读数与包真身同判）
- **裁决：整改真实有效**
- 必须提的整改项：
  1. `update.ts:224` 的 `.catch(() => {})` 吞掉 `store.load()` 失败后，`.then` 仍按内存默认值排定时器 ⇒ load 失败时会把「上次关掉过」的用户按默认 `autoCheckEnabled: true` 重新打开（与注释自认的风险同类，属既存小口子，非本次回归）。
  2. 建议把 M3a 教训固化进 verify：让位分支断言应同时钉住「不早退**也不抛**」，否则「因错理由变红」的变异仍会被误读为证据。
  3. 本次未验证：真装机（`install`）路径、真实 npm 源与真实宿主 `ctx.interval/ctx.timeout` 行为——三通电话中的 `install` 在 verify 内按设计只走失败通道。
