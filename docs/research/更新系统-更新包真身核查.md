# 插件更新系统 · 更新包 dsh-plugin-update@0.1.1 真身核查

> 核查对象：npm 官方源发布产物 `dsh-plugin-update@0.1.1`（tarball shasum `d49573d37bb273f10abcdf9062227b4710fa5765`，14 文件）。
> 取法：仓外临时目录 `npm pack` + 解包 + 真机 `import` 跑通，**未动本仓**。
> 行号指向包内构建产物 `dist/*.js`（**不是** TS 源；README 里引的 `reader.ts:212` / `commands.ts:105` 是源行号，与产物不同）。
> 凡无出处者一律标 `未证实`。README 快照 `.tmp-wayfinder/dsh-plugin-update-readme.md` 经重解码后**与包内 README.md 逐字节相同**（19903 B），故漂移全部是「README ↔ 代码」，不是「快照 ↔ 发布文档」。

## 权威事实表

### 1. package.json（包内 `package.json`）

- `version` `0.1.1`（:3）；`type` `module`（:10）；`engines.node` `>=22`（:7-9）。
- 入口：`exports` 只有 `.` → `./dist/host.js` 与 `./package.json`（:11-14）。**没有 `main`、也没有 `module`** —— 不是「双入口导出」，是单入口 + 被 exports 封住的其余文件。
- 运行时依赖：**零** —— 全文无 `dependencies`；只有 `devDependencies.typescript 5.6.3`（:31-33）。
- `files` **5 项**（:15-21）：`dist`、`derive-client-values.mjs`、`event-list.template.json`、`README.md`、`LICENSE`。
- `dist` 下 9 个 JS：`client.js commands.js config.js gate.js host.js ports.js reader.js service.js store.js`（`npm pack` 清单核对无误）。

### 2. 导出清单（实测 `Object.keys(await import('dsh-plugin-update'))`，共 17 个）

`__resetSharedUpdateReaderForTests`、`buildPhoneName`、`buildPhoneNames`、`checkEventCounts`、`checkEventFields`、`containingPackage`、`createHostUpdate`、`createUpdateDiskPorts`、`createUpdateExecutor`、`defaultHomeDir`、`detectEnvironmentKind`、`parseEventListManifest`、`pathsForUpdate`、`profileNameValid`、`registrySpec`、`resolveCliEntry`、`resolveUpdateConfig`（`host.js:261-279`）。

**`createClientUpdate` 不存在** —— 包根没有，`dist/client.js` 没有，全 README 出现 0 次。
客户端侧 `dist/client.js` 有 6 个导出：`CLIENT_POLL`、`assertPollInterval`、`buildClientPhoneNames`、`buildPhoneName`、`buildPhoneNames`、`manualCommand`（`client.js:18-25`）。

### 3. createHostUpdate 的真实签名与选项

`createHostUpdate(deps = {}, configInput)`（`host.js:212`）—— 第一个参数是 deps 不是 ctx：

- `deps.ctx`（`host.js:216`）、`deps.logCtx`（:215）、`deps.desktopPnpm`（:217）、`deps.readerOverrides`（:219，**README 未写，实测存在且可用于测试替身**）。
- `configInput` **必填**：`pluginId` 必填（`config.js:17-25`，非空且不含 `/` `\` `\0`）；`prefix` 默认 `"wf"`（`config.js:2,46`，且不得含点/路径分隔符/空白）；`targetPackageName` 默认 `"dsh-mattpocock-skills-deck"`（:4,47）；`registryUrl` 默认 **`https://registry.npmjs.org/`**（:5,51）；`homeDir` 默认 `null` → 回落 `DSH_HOME` 否则 `~/.dsh`（`reader.js:72-76`）；`checkTimeoutMs` 10000（:11,56）；`confirmationTtlMs` 600000（:12,57）；`installTimeoutMs` 900000（:13,58）；`panelPollMs` 1000（:14,59）。实测 `resolveUpdateConfig({pluginId:'x'})` 全部默认值如上。
- 返回 **`{ phoneNames, handlers }`**（`host.js:251`）——只有两个键。

### 4. 电话名（handler map 真实键）

`buildPhoneNames(prefix)`（`config.js:65-72`）返回**对象**：`{ updateStatus: prefix+'.updateStatus', updateCheck: prefix+'.updateCheck', updateInstall: prefix+'.updateInstall' }`。
handler 键即这三个全名（`host.js:246-250`）。实测 `prefix:'imc'` → `update.phoneNames = {"updateStatus":"imc.updateStatus","updateCheck":"imc.updateCheck","updateInstall":"imc.updateInstall"}`，`Object.keys(update.handlers)` 逐字相同。**任务假设的 `imc.updateStatus/imc.updateCheck/imc.updateInstall` 成立。**

### 5. blockedReason 的真实枚举

**包内不存在枚举常量数组。** `blockedReason` 是各赋值点直接产出的裸字符串；快照里还是**无校验透传**（`service.js:209`：`env.blockedReason ?? checked?.blockedReason ?? null`）。全量赋值点（`dist` 全文 grep）：

| 取值 | 赋值点 |
|---|---|
| `unknown-profile` | `reader.js:120`、`reader.js:129`（另有 `host.js:85` 走错误通道） |
| `invalid-installation` | `reader.js:140`、`reader.js:159` |
| `installation-changed` | `reader.js:157`、`reader.js:158` |
| `source-install` | `reader.js:160` |
| `pending-restart` | `reader.js:161`、`service.js:213` |
| `recovery-required` | `service.js:211` |
| `incompatible-node` | `service.js:253` |
| `registry-conflict` | **零赋值点** —— 全包仅 3 处出现：`host.js:154`（错误码白名单）、`service.js:284`（job.message 候选）、`README.md:201`。无任何代码路径会把它写成 blockedReason。 |

即「8 种」中真正可达的只有 **7 种**。

### 6. sourceInstall 的判据

`reader.js:144`：
`result.sourceInstall = !registrySpec(deps[targetPackageName]) || !inside(join(profileDir,"node_modules"), installed.directory)`

输入两项：(a) 使用范围目录 `package.json` 的 `dependencies[targetPackageName]` 版本说明符，用 `registrySpec` 判是否为「版本号/范围符/纯标识」（`reader.js:43-45`）——`file:`、`link:`、`workspace:` 一律判为源安装；(b) 已装包目录 realpath 是否落在 `<profileDir>/node_modules` 之内（`reader.js:10-13`）。
注：README 引的 `reader.ts:212` 对应产物 `reader.js:144`。

### 7. 手工兜底命令的生成

- 字段名 `manual`，由 `snapWithManual`（`host.js:177-183`）注入每次 status/check 回包；是字符串或 `null`。
- **不给命令的条件恰为两个 blockedReason**：`commands.js:57` `if (input.sourceInstall || input.blockedReason === "source-install" || input.blockedReason === "unknown-profile") return null`（另有范围名不可用时返回 null，`commands.js:31-36,58-59`）。与 README 第 9 节一致。
- 形状（`commands.js:74`）：`dsh plugin --profile ${arg} add --save-exact ${targetName}@${version} --registry=${registry}`。实测：`dsh plugin --profile web add --save-exact dsh-im-companion@9.9.9 --registry=https://registry.npmjs.org/`。范围名含非法字符时 `JSON.stringify` 加引号（:63）。
- 版本选取：`picks = [latestVersion, jobTargetVersion, installedVersion]` 过滤三段版本（:64）；全空则回退字面量 `latest`（:65）；再按 ≥ runningVersion 取最大（:66-71）。

### 8. 状态快照与 receipt

快照**恰好六个字段**（`service.js:221-228`）：`runningVersion`、`installedVersion`、`latestVersion`、`canInstall`、`blockedReason`、`job`。实测逐字一致。
`job` 形状（`service.js:313-319`、`store.js:54-62`）：`{ id, state, targetVersion, message, requestId }`。
**receipt 形状**（`service.js:266-269`）：`{ checkId, checkedAt, expiresAt }`。
外层信封（`host.js:204,208`）：成功 `{ ok:true, snapshot, manual, receipt }`；失败 `{ ok:false, error, errorKind }`。
**关键**：receipt 只在 `updateCheck` 且 `snapshot.canInstall === true` 时非空（`service.js:240,256`）；`updateInstall` 与 `updateStatus` 的 receipt **恒为 `null`**（实测）。

### 9. 错误码

`host.js:142-156` 白名单 13 个：`check-failed`、`invalid-release`、`check-expired`、`update-busy`、`install-failed`、`unknown-profile`、`source-install`、`invalid-installation`、`installation-changed`、`pending-restart`、`incompatible-node`、`registry-conflict`、`recovery-required`。不在表内一律折叠为 `{ error:"check-failed", errorKind:"internal" }`（`host.js:158`）。
产出点：`invalid-release` `service.js:121,123,125,128,135,139`；`check-failed` `:116,119,145`；`check-expired` `:299,306,323`；`update-busy` `:304,320`；`install-failed` `:166,174,278,279`；`installation-changed` `:309,325`。
**与 blockedReason 同名的 token 也会从错误通道回来**：`service.js:310-311` 与 `:326` 直接 `throw updateError(env.blockedReason)`；实测环境转为 source-install 后再装 → `{"ok":false,"error":"source-install","errorKind":"source-install"}`。

### 10. 轮询

**包内部不轮询** —— `dist` 全文无 `setInterval`；唯一定时器是安装子进程超时（`store.js:249`）。包只给口径：
`DEFAULT_PANEL_POLL_MS = 1e3`、`MIN_PANEL_POLL_MS = 250`（`config.js:14,16`）；配置 `panelPollMs` 默认 1000，<250 抛错（`config.js:59-62`，实测 200 抛中文错）；客户端侧 `CLIENT_POLL = { defaultMs: 1000, minMs: 250 }`（`client.js:5-8`）与 `assertPollInterval`（`client.js:12-17`，实测 200 抛错、250 通过）。**轮询循环由面板自己写**（README 示例用 `setInterval(readStatus, UPD_POLL)`）。

### 11. 「2 秒内重复点击复用上次结果」

常量 `RECHECK_WINDOW_MS = 2e3`，**两份**：`config.js:15` 与 `service.js:6`；判定在 `service.js:237` `if (checked?.checkId && ports.now() - lastCheckAt < RECHECK_WINDOW_MS)`。
实测：2 秒内第二次 `updateCheck` 不发网络请求、`checkId` 不变；超窗重新拉取。（另有安装终止宽限 `TERMINATION_GRACE_MS = 3e3`，`store.js:178`。）

### 12. 状态机真实取值

`JOB_STATES = ["installing","verifying","restart-required","completed","failed","interrupted"]`（`store.js:9`，共 **6 个**）。
实测六个均可达成：`installing` 起手（`service.js:315`）→ `verifying`（:274-276）→ `restart-required`（:280）→ 运行版本追平后 `completed`（:204）；`interrupted`（:197,:201）；`failed`（:285，message 取 `install-failed`）。

## README 与产物的漂移清单

1. **【最严重】README §8**：「装不了时原因**一定是下面八种之一**，照表处理，不要自己猜。」/ 表内 `registry-conflict`「本地声明的版本与磁盘实际版本互相矛盾」
   ↔ 产物：`registry-conflict` **无任何赋值点**（全包 3 处出现均非 blockedReason，见事实 5）。实际可达 7 种。**照 README 写 8 条文案，其中 1 条永远触发不了；反过来若真收到该串，说明集成方自己塞进来的。**
2. **【最严重】README §8/§9 把 blockedReason 与错误码当互斥通道**：「`blockedReason` 说装不了的原因」/「能给则给、不能给则为空」
   ↔ 产物：同一 token 也会以 `{ok:false, error:'source-install', errorKind:'source-install'}` 返回（`service.js:310-311,326`，已实测）。README 通篇未写错误通道。
3. **README §2**：「`dist/client.js`（客户端侧用）：电话名拼法、轮询时间口径、手工兜底命令形状。」
   ↔ 产物：`exports` 只暴露 `.` 与 `./package.json`（`package.json:11-14`），实测 `import 'dsh-plugin-update/dist/client.js'` 报 `ERR_PACKAGE_PATH_NOT_EXPORTED`。该文件**只能按文件路径引用**（derive 工具即如此，`derive-client-values.mjs:87`）—— 「双入口」的措辞会诱导集成方写子路径 import。
4. **README §1**：「要求 Node 22 或更高。**当前版本 `0.1.0`**。零运行时依赖。」
   ↔ 产物 `package.json:3` `"version": "0.1.1"`。包内 README 版本号未随发布更新。
5. **README §7**：「发布白名单（`files`）**共 4 项**（`dist`、`event-list.template.json`、`README.md`、`LICENSE`…）」
   ↔ 产物 `files` **5 项**，多出 `derive-client-values.mjs`（`package.json:15-21`）——正是 §3 第 3 步让集成方跑的那个工具。
6. **README §3 第 1 步**：「`npm install dsh-plugin-update`」（未给安装位置要求）
   ↔ 产物要求该包必须位于**目标插件包之内**：`containingPackage` 从 `host.js` 向上找 `name === targetPackageName`（`host.js:83`、`reader.js:26-39`）。实测平级安装（`<profile>/node_modules/dsh-plugin-update`）→ `{"ok":false,"error":"unknown-profile"}`；嵌在 `<profile>/node_modules/dsh-im-companion/node_modules/` 内才正常返回快照。**README 缺这条硬前提。**
7. **README §3 第 2 步**：deps 只写 `{ ctx, logCtx }`
   ↔ 产物还认 `deps.desktopPnpm`（`host.js:217`）与 `deps.readerOverrides`（`host.js:219`，注入 `runningVersion/profileDir/fetchImpl/now/readInstalled/readJob/writeJob/tryAcquireLock/runInstall` 等）。README 未写。
8. **README §9/§11 从未出现 `receipt` 一词**（全 README 0 次）
   ↔ 产物回包第二个载荷字段就是 `receipt`（`{checkId,checkedAt,expiresAt}`，`service.js:266-269`），且只由 `updateCheck` 在 `canInstall===true` 时给出（实测 `updateInstall` 恒 null）。凭证（`check-expired` 的 checkId）口径无文档。
9. **README §3 第 3 步**未在第 1 步的安装命令里提示需要 esbuild
   ↔ 产物零依赖、devDependencies 仅 typescript（`package.json:31-33`），derive 工具必然走 `loadEsbuild()` 失败分支提示 `npm install --save-dev esbuild`（`derive-client-values.mjs:68-82`）。README 只在同级 bullet 里提了一句「会打印安装提示」。**属缺漏，不算错。**

一致项（不列）：快照六字段、三种电话名、2 秒复用窗、3 秒终止宽限、`createHostUpdate` 两参形态、8 项配置默认值与下界、`manual` 的两种空情形、`manualCommand` 命令形状、`pending-restart` 期间 `canInstall` 为假、`update.phoneNames` 读取建议 —— 均已逐条在产物中验对。

## 本仓集成可用性判定

**结论：README §3 第 2 步可照抄；第 3 步不可照「子路径 import」理解，必须走它自带的 derive 工具；另有三条 README 未写、但实测必须满足的硬前提。**

| README §3 步骤 | 能否照抄 | 依据 |
|---|---|---|
| 第 2 步 宿主侧 `createHostUpdate` + 遍历 `handlers` 注册 | **可以** | `host.js:212-251`，实测键为 `imc.updateStatus/imc.updateCheck/imc.updateInstall` |
| 第 3 步 客户端侧取名 | **不能按字面照抄** | 子路径被 `exports` 拦（`ERR_PACKAGE_PATH_NOT_EXPORTED`）；只能用 derive 工具按绝对路径打包，且需自备 esbuild |

README 未写但必须满足的硬前提（**第 1 条已被第二轮核查推翻改写，以文末「追加核查（第二轮）」为准**）：
1. `dsh-plugin-update` 必须装在 `dsh-im-companion` 自己的 `node_modules` 下。~~（junction 挂载本仓时即成立）~~ —— 真实 profile 的 pnpm `nodeLinker: hoisted` 会把普通 `dependencies` 拉到平级，届时**更新系统直接失效**；必须用 `bundleDependencies` 把依赖嵌进本仓发布产物。
2. 本仓 package.json 须同时具备三个可解析入口 `main`、`exports["./client"]`、`dsh.bundle.patch`（`reader.js:46-70` `validPackage`；本仓为 `./lib/index.js`、`./lib/client.js`、`./cordis.patch.yml`，三文件均存在）。
3. 使用范围目录的 `package.json` 里，本包依赖说明符必须是版本号/范围（不能 `file:`/`link:`/`workspace:`），否则恒判 `source-install`（`reader.js:43-45,144`）。

可直接用的代码骨架（出处标在注释）：

```js
// ── 宿主侧 lib/index.js ────────────────────────── 出处 host.js:212-251
import { createHostUpdate } from 'dsh-plugin-update'   // 包根 = 唯一可 import 的入口

const update = createHostUpdate(
  { ctx, logCtx },
  { pluginId: 'dsh-im-companion', prefix: 'imc', targetPackageName: 'dsh-im-companion' }
)
for (const [name, handler] of Object.entries(update.handlers)) registry.set(name, handler)
// update.phoneNames = { updateStatus:'imc.updateStatus', updateCheck:'imc.updateCheck', updateInstall:'imc.updateInstall' }
// 别自己拼字符串，也别 import 'dsh-plugin-update/dist/client.js'（被 exports 拦）

// ── 客户端侧（面板）────────────────────────────── 出处 client.js:5-17 + derive-client-values.mjs
// 构建期跑一次（需自备 esbuild：npm i -D esbuild）：
//   node node_modules/dsh-plugin-update/derive-client-values.mjs \
//        --prefix imc --out scripts/generated/updateClient.derived.js
// 产出 UPD_STATUS / UPD_CHECK / UPD_INSTALL / UPD_POLL(=1000) / UPD_POLL_MIN(=250)，面板直接引用

// ── 两端统一判读 ───────────────────────────────── 出处 host.js:204-208、service.js:221-228
// 成功 → { ok:true, snapshot:{runningVersion,installedVersion,latestVersion,canInstall,blockedReason,job}, manual, receipt }
// 失败 → { ok:false, error, errorKind }   ← 注意：blockedReason 同名 token 也会从这里来
```

## 8 种 blockedReason 逐字取值表

> 原始字符串逐字照抄代码；含义只做事实转写，**中文用户文案由另一 agent 定，本表不提供**。

| 原始字符串 | 代码出处 | 一句话含义 |
|---|---|---|
| `unknown-profile` | `reader.js:120,129`；`host.js:85`（错误通道） | 使用范围认不出：范围名非法，或 `homeDir`/`profileDir` 的 realpath 解析失败，或从 host.js 向上找不到名为 `targetPackageName` 的包（runningVersion 解析不出）。 |
| `source-install` | `reader.js:160` | 当前是按源码装的：范围清单里本包说明符不是版本号/范围，或已装目录 realpath 不在 `<profileDir>/node_modules` 之内。 |
| `invalid-installation` | `reader.js:140,159` | 已装包不完整：范围目录或已装包的 package.json 读不到；或包名对不上、版本非 `x.y.z`、三个入口（`main`/`exports["./client"]`/`dsh.bundle.patch`）缺失或越出包目录。 |
| `installation-changed` | `reader.js:157,158` | 安装指纹（homeDir + profileDir + 范围名 + pluginId）中途变了，或当前已加载的包与磁盘已装包不是同一份。 |
| `pending-restart` | `reader.js:161`；`service.js:213` | 磁盘已装版本 ≠ 正在运行版本：新版已落盘但仍跑旧版（正常终态）。 |
| `incompatible-node` | `service.js:253` | 新版 `engines.node` 与当前 Node 对不上（`satisfiesNodeRange` 判定；含 `*`、`||` 或缺省一律视为通过）。 |
| `recovery-required` | `service.js:211` | 上次安装被打断：残留 `installing`/`verifying`/`interrupted`/`failed` 任务，且磁盘版本 ≠ 运行版本。 |
| `registry-conflict` | **无产出路径**（仅 `host.js:154`、`service.js:284`、`README.md:201`） | **产物中没有任何代码会把它写成 blockedReason** —— 不是「本地声明版本与磁盘版本矛盾」这个语义，而是死值。 |
